import styled from 'styled-components';

import colWidthMixin from '@mixins/colWidth';
import relativeMixin from '@mixins/relative.mixin';

export default styled.div`
  ${relativeMixin}
  ${colWidthMixin}
`;
